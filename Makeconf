AR=ar
BLAS=blas.o
CC=gcc
CFLAGS=-g -O2
CPICFLAGS=-fpic
DLLFLAGS=
FC=g77
FFLAGS=-O2
FPICFLAGS=-fpic
F2C=
F2CFLAGS=
INSTALL=/users/rdev/ihaka/R/etc/install-sh -c
INSTALL_DATA=${INSTALL} -m 644
INSTALL_PROGRAM=${INSTALL}
LDCMD=g77
LDFLAGS=
LIBS=-lreadline -ldl -ltermcap -lm 
RANLIB=ranlib
SHLIBLDFLAGS=-G
SHELL=/bin/sh
X_CFLAGS= -I/usr/local/X11R6/include
X_LIBS= -L/usr/local/X11R6/lib -R/usr/local/X11R6/lib -lX11
X_PRE_LIBS= -lSM -lICE
X_EXTRA_LIBS=-lsocket  -lnsl
YACC=bison -y



.f.c:

.f.o:
	$(FC) $(FFLAGS) -c $< -o $@

.c.o:
	$(CC) $(CFLAGS) $(INCLUDES) -c $<

prefix=/usr/local
exec_prefix=${prefix}
bindir=${exec_prefix}/bin
libdir=${exec_prefix}/lib
mandir=${prefix}/man
rhome=${exec_prefix}/lib/R
