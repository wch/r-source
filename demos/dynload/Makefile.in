CC=@CC@
CFLAGS=@CFLAGS@ @CPICFLAGS@
LD=ld
SHLIBLDFLAGS=@SHLIBLDFLAGS@

all:		zero.so

zero.so:	zero.o
		$(LD) $(SHLIBLDFLAGS) zero.o -o zero.so

zero.o:		zero.c
		$(CC) $(CFLAGS) -c zero.c

clean:
		@rm -f *.o *.so

realclean:	clean
		@rm -f Makefile
