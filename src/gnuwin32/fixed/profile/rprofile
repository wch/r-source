.Library <- file.path(getenv("RHOME"), "library")

.lib.loc <- unique(c(unlist(strsplit(getenv("RLIBS"), ";")), .Library))

options(papersize="a4")
options(device="X11")
options(editor="notepad")
options(pager="internal")
options(unzip="internal")
options(width=80)
