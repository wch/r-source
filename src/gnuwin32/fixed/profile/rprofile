###-*- R -*-

.Library <- file.path(getenv("R_HOME"), "library")
.lib.loc <- unique(c(unlist(strsplit(c(getenv("R_LIBS"),
                                       getenv("RLIBS")), ";")), .Library))
options(papersize="a4")
options(device="windows")
options(editor="notepad")
options(pager="internal")
options(unzip="internal")
options(width=80)
