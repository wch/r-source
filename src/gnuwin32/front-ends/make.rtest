include ../MkRules

ifdef DEBUG
  DEBUGFLAG=-g
  LINKFLAGS=
else
  DEBUGFLAG=
  LINKFLAGS=-s
endif

LIBS=-L.. -lR
CFLAGS=-Wall

rtest-CFLAGS=-I.. -I../../include
Rtest.exe: rtest.o R.dll
	$(CC) -o $@ rtest.o -L.. -lR

rtest.c: ../../include/Rversion.h

R.dll: ../R.dll
	$(CP) -p $^ $@

clean:
	$(RM) rtest.o Rtest.exe *~ R.dll


