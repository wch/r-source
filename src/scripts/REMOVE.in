#!@R_SHELL@
#
# ${R_HOME}/bin/REMOVE for removing add-on packages

echo "tools:::.remove_packages()" | \
  R_DEFAULT_PACKAGES=NULL ${R_HOME}/bin/R --slave --args $@

### Local Variables: ***
### mode: sh ***
### sh-indentation: 2 ***
### End: ***
