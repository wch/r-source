###-*- R -*- MS-Windows Specific ----
>>>>>>> 1.1.1.1.6.2
.Library <- paste(getenv("RHOME"), "library", sep = "\\")

.lib.loc <- unique(c(unlist(strsplit(getenv("RLIBS"), ":")), .Library))

options(papersize = "a4")

.First <- function() windows()
