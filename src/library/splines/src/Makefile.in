#
# ${R_HOME}/src/library/splines/src/Makefile

pkg = splines

VPATH = @srcdir@
srcdir = @srcdir@
top_srcdir = @top_srcdir@

top_builddir = ../../../..
subdir = src/library/$(pkg)/src
R_HOME = $(top_builddir)

SHLIB = $(pkg).@SHLIBEXT@
OBJS = splines.o

all: Makefile $(SHLIB)
	@$(top_srcdir)/tools/mkinstalldirs \
	  $(top_builddir)/library/$(pkg)/libs
	@$(top_srcdir)/tools/copy-if-change $(SHLIB) \
	  $(top_builddir)/library/$(pkg)/libs/$(SHLIB)

Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@cd $(top_builddir) && \
	  CONFIG_FILES=$(subdir)/$@ CONFIG_HEADERS= \
	  $(SHELL) ./config.status

include $(top_builddir)/etc/Makeconf

mostlyclean: clean
clean:
	@rm -f *.o *.$(SHLIBEXT)
distclean: clean
	@rm -f Makefile
maintainer-clean: distclean
