#! /bin/sh
# ${RHOME}/src/library/LIBSETUP
# Set up packages (by default, all but `base' and `profile')

RHOME=${RHOME:-`cd ../..; pwd`}
PKGS=`ls|sed '
/CVS/d
/LIBCLEAN/d
/LIBSETUP/d
/Makefile/d
/base/d
/profile/d
'`

export RHOME
for pkg in ${PKGS}
do
    sh ${RHOME}/etc/INSTALL -help ${pkg}
done
