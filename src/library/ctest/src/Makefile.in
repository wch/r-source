#
# ${R_HOME}/src/library/ctest/src/Makefile

pkg = ctest

VPATH = @srcdir@
srcdir = @srcdir@
top_srcdir = @top_srcdir@

top_builddir = ../../../..
subdir = src/library/$(pkg)/src
R_HOME = $(top_builddir)

OBJS = ansari.o d2x2xk.o fexact.o kendall.o ks.o prho.o swilk.o
SHLIB = $(pkg).@SHLIB_EXT@

## FIXME:
## We should really add FLIBS to SHLIB_LIBADD, but we need to include
## the shlib Makeconf at a later stage, and its default value would
## override our setting.
PKG_LIBS = @FLIBS@
## </FIXME>

all: Makefile $(SHLIB)
	@$(top_srcdir)/tools/mkinstalldirs \
	  $(top_builddir)/library/$(pkg)/libs
	@$(top_srcdir)/tools/copy-if-change $(SHLIB) \
	  $(top_builddir)/library/$(pkg)/libs/$(SHLIB)

Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@cd $(top_builddir) && \
	  CONFIG_FILES=$(subdir)/$@ CONFIG_HEADERS= \
	  $(SHELL) ./config.status

include $(top_builddir)/etc/Makeconf

mostlyclean: clean
clean:
	@-rm -rf .libs _libs
	@-rm -f *.o *.$(SHLIB_EXT)
distclean: clean
	@-rm -f Makefile
maintainer-clean: distclean
