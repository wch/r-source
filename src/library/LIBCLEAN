#! /bin/sh
# ${RHOME}/src/library/LIBCLEAN

RHOME=${RHOME:-`cd ../..; pwd`}
PKGS=`ls|sed '
/CVS/d
/LIBCLEAN/d
/LIBSETUP/d
/Makefile/d
/base/d
/profile/d
'`

for pkg in ${PKGS}
do
    if test -d ${pkg}/src
    then
	(cd ${pkg}/src; rm -f *.o *.so)
    fi
done
