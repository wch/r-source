#-*- Makefile -*-
## need config.h and Rgui and GraphApp headers.
PKG_CPPFLAGS=-I$(RHOME)/src/include -I$(RHOME)/src/extra/zlib -I$(RHOME)/src/gnuwin32 -I$(RHOME)/src/extra -DHAVE_CONFIG_H
ifdef SUPPORT_UTF8
PKG_CPPFLAGS+=-DSUPPORT_UTF8
endif

PKG_LIBS=$(RHOME)/src/extra/zlib/libz.a -lgraphapp

## need to avoid quartz files added for 2.7.0
OBJECTS=chull.o devNull.o devPicTeX.o devPS.o devWindows.o init.o
EXTRA_LIBS=
