export(Hershey, check.options, col2rgb, colors, colours, dev.control,
       dev.copy, dev.copy2eps, dev.cur, dev.interactive, dev.list,
       dev.next, dev.off, dev.prev, dev.print, dev.set, graphics.off,
       gray, grey, hsv, palette, pdf, pictex, postscript, ps.options,
       rainbow, recordPlot, replayPlot, rgb, rgb2hsv, xfig)

export(cm.colors, heat.colors, terrain.colors, topo.colors)
export(postscriptFont, postscriptFonts)

S3method(print, recordedplot)

if(tools:::.OStype() == "unix") {
export(GNOME, X11, bitmap, dev2bitmap, gnome, jpeg, png, quartz, x11,
       X11Font, X11Fonts)
}

if(tools:::.OStype() == "windows") {
export(X11, bitmap, bmp, bringToTop, dev2bitmap, jpeg, png, savePlot,
       win.graph, win.metafile, win.print, windows, x11)

S3method("[", SavedPlots)
S3method(print, SavedPlots)
}
