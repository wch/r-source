useDynLib(stats)

# exportPattern("^[^\\.]")
export(.cbind.ts, .checkMFClasses, .getXlevels, .MFclass)

export(add.scope, add1, add1.default, add1.glm, add1.lm, add1.mlm,
       aggregate, aggregate.data.frame, aggregate.default,
       aggregate.ts, AIC, AIC.default, AIC.logLik, alias,
       alias.formula, alias.lm, anova, anova.glm, anova.glmlist,
       anova.lm, anova.lmlist, anova.mlm, aov, as.data.frame.logLik,
       as.formula, as.table.ftable, as.ts, ave, binomial, bw.bcv,
       bw.nrd, bw.nrd0, bw.SJ, bw.ucv, C, case.names,
       case.names.default, case.names.lm, cbind.ts, coef, coef.aov,
       coef.default, coef.listof, coefficients, complete.cases,
       confint, confint.lm, constrOptim, contr.helmert, contr.poly,
       contr.sum, contr.treatment, contrasts, "contrasts<-",
       cooks.distance, cooks.distance.glm, cooks.distance.lm, cor,
       cov, cov.wt, cov2cor, covratio, cycle, cycle.default, cycle.ts,
       D, dbeta, dbinom, dcauchy, dchisq, delete.response, deltat,
       deltat.default, density, deriv, deriv.default, deriv.formula,
       deriv3, deriv3.default, deriv3.formula, deviance,
       deviance.default, deviance.glm, deviance.lm, deviance.mlm,
       dexp, df, df.residual, df.residual.default, dfbeta, dfbeta.lm,
       dfbetas, dfbetas.lm, dffits, dgamma, dgeom, dhyper, diff.ts,
       dlnorm, dlogis, dmultinom, dnbinom, dnorm, dpois, drop.scope,
       drop.terms, drop1, drop1.default, drop1.glm, drop1.lm,
       drop1.mlm, dsignrank, dt, dummy.coef, dummy.coef.aovlist,
       dummy.coef.lm, dunif, dweibull, dwilcox, eff.aovlist, effects,
       effects.glm, effects.lm, end, end.default, expand.model.frame,
       extractAIC, extractAIC.aov, extractAIC.coxph, extractAIC.glm,
       extractAIC.lm, extractAIC.negbin, extractAIC.survreg,
       factor.scope, family, family.glm, family.lm, fitted,
       fitted.default, fitted.values, fivenum, formula,
       formula.data.frame, formula.default, formula.formula,
       formula.lm, formula.terms, frequency, frequency.default,
       ftable, ftable.default, ftable.formula, Gamma, gaussian, glm,
       glm.control, glm.fit, hasTsp, hat, hatvalues, hatvalues.lm,
       influence, influence.glm, influence.lm, influence.measures,
       integrate, interaction.plot, inverse.gaussian, IQR,
       is.empty.model, is.mts, is.ts, lines.ts, lm, lm.fit,
       lm.influence, lm.wfit, logLik, logLik.glm, logLik.lm, loglin,
       lowess, ls.diag, ls.print, lsfit, mad, mahalanobis, make.link,
       make.tables.aovproj, make.tables.aovprojlist, makepredictcall,
       makepredictcall.default, makepredictcall.poly, manova, mean,
       mean.data.frame, mean.default, median, model.extract,
       model.frame, model.frame.aovlist, model.frame.default,
       model.frame.glm, model.frame.lm, model.matrix,
       model.matrix.default, model.matrix.lm, model.offset,
       model.response, model.tables, model.tables.aov,
       model.tables.aovlist, model.weights, na.action,
       na.action.default, na.exclude, na.exclude.data.frame,
       na.exclude.default, na.fail, na.fail.default, na.omit,
       na.omit.data.frame, na.omit.default, na.omit.ts, na.pass,
       napredict, napredict.default, napredict.exclude, naprint,
       naprint.default, naprint.exclude, naprint.omit, naresid,
       naresid.default, naresid.exclude, nlm, offset, Ops.ts, optim,
       optimise, optimize, p.adjust, p.adjust.methods, pbeta, pbinom,
       pcauchy, pchisq, pexp, pf, pgamma, pgeom, phyper, plnorm,
       plogis, plot.density, plot.lm, plot.mlm, plot.ts,
       plot.TukeyHSD, pnbinom, pnorm, poisson, poly, polym, power,
       ppoints, ppois, predict, predict.glm, predict.lm, predict.mlm,
       predict.poly, preplot, printCoefmat, print.anova,
       print.aov, print.aovlist, print.density,
       print.dummy.coef, print.dummy.coef.list, print.family,
       print.formula, print.ftable, print.glm, print.infl,
       print.integrate, print.lm, print.logLik, print.mtable,
       print.summary.aov, print.summary.aovlist, print.summary.glm,
       print.summary.lm, print.summary.manova, print.tables.aov,
       print.terms, print.ts, print.TukeyHSD, profile, proj, proj.aov,
       proj.aovlist, proj.default, proj.lm, psignrank, pt, ptukey,
       punif, pweibull, pwilcox, qbeta, qbinom, qcauchy, qchisq, qexp,
       qf, qgamma, qgeom, qhyper, qlnorm, qlogis, qnbinom, qnorm,
       qpois, qqline, qqnorm, qqnorm.default, qqplot, qsignrank, qt,
       qtukey, quantile, quantile.default, quasi, quasibinomial,
       quasipoisson, qunif, qweibull, qwilcox, rbeta, rbinom, rcauchy,
       rchisq, read.ftable, reformulate, relevel, relevel.default,
       relevel.factor, relevel.ordered, replications, reshape, resid,
       residuals, residuals.default, residuals.glm, residuals.lm,
       rexp, rf, rgamma, rgeom, rhyper, rlnorm, rlogis, rmultinom,
       rnbinom, rnorm, rpois, rsignrank, rstandard, rstandard.glm,
       rstandard.lm, rstudent, rstudent.glm, rstudent.lm, rt, runif,
       rweibull, rwilcox, sd, se.aov, se.aovlist, se.contrast,
       se.contrast.aov, se.contrast.aovlist, spline, splinefun, start,
       start.default, stat.anova, step, str.logLik, summary.aov,
       summary.aovlist, summary.glm, summary.infl, summary.lm,
       summary.manova, summary.mlm, symnum, t.ts, termplot, terms,
       terms.aovlist, terms.default, terms.formula, terms.terms, time,
       time.default, time.ts, ts, tsp, "tsp<-", TukeyHSD, TukeyHSD.aov,
       uniroot, update, update.default, update.formula, var,
       variable.names, variable.names.default, variable.names.lm,
       vcov, vcov.coxph, vcov.glm, vcov.gls, vcov.lm, vcov.lme,
       vcov.survreg, weighted.mean, weighted.residuals, weights,
       weights.default, weights.glm, window, window.default,
       window.ts, write.ftable)

importFrom(graphics, plot)
importFrom(utils, str)

S3method("[", formula)
S3method("[", terms)
S3method("[", ts)
S3method(print, htest)

## from ctest
exportPattern("\\.test$")
export(pairwise.table)

# documented but not exported
# export(print.power.htest)

S3method(ansari.test, default)
S3method(ansari.test, formula)
S3method(bartlett.test, default)
S3method(bartlett.test, formula)
S3method(cor.test, default)
S3method(cor.test, formula)
S3method(fligner.test, default)
S3method(fligner.test, formula)
S3method(friedman.test, default)
S3method(friedman.test, formula)
S3method(kruskal.test, default)
S3method(kruskal.test, formula)
S3method(mood.test, default)
S3method(mood.test, formula)
S3method(quade.test, default)
S3method(quade.test, formula)
S3method(t.test, default)
S3method(t.test, formula)
S3method(var.test, default)
S3method(var.test, formula)
S3method(wilcox.test, default)
S3method(wilcox.test, formula)

## from modreg
export(isoreg, ksmooth, loess, loess.control, loess.smooth, ppr,
       runmed, scatter.smooth, smooth.spline, smoothEnds, supsmu)

# methods which are documented and registered, but not exported
# export(plot.isoreg, plot.ppr,
#        predict.loess, predict.smooth.spline)

S3method(anova, loess)
S3method(fitted, isoreg)
S3method(plot, isoreg)
S3method(plot, ppr)
S3method(ppr, default)
S3method(ppr, formula)
S3method(predict, loess)
S3method(predict, ppr)
S3method(predict, smooth.spline)
S3method(predict, smooth.spline.fit)
S3method(print, isoreg)
S3method(print, loess)
S3method(print, ppr)
S3method(print, smooth.spline)
S3method(print, summary.loess)
S3method(print, summary.ppr)
S3method(residuals, isoreg)
S3method(summary, loess)
S3method(summary, ppr)
