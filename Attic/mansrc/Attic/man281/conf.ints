TITLE(conf.ints @@ Estimate Proportions)
USAGE(conf.ints(counts))
DESCRIPTION(
LANG(conf.ints) calculates 95 percent confidence intervals
for the population proportions in a one-dimensional
contingency table.
It takes as input a vector LANG(x) which contains the
observed counts in each category and
returns a two-column matrix containing the
upper and lower endpoints of the intervals.
)
