TITLE(levene.test @@ Levene Test)
USAGE(
levene.test(data, group)
)
ARGUMENTS(
ARG(data @@ a vector containing the data.)
ARG(group @@ a vector of group labels.)
)
DESCRIPTION(
Performs a Levene test for equal group variances
in one-way ANOVA and prints out the results.
)
