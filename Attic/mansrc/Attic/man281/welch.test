TITLE(welch.test @@ Welch Test)
USAGE(
welch.test(data, group, print.out=true,
        conf.level=95, data.order=true)
)
ARGUMENTS(
ARG(data @@ the data values.)
ARG(group @@ a vector of group labels.)
ARG(print.out @@ if LANG(true), printing is suppressed.)
ARG(conf.level @@ confidence level of the interval, expressed as a percentage.)
ARG(data.order @@ determines the order of the groups.
If LANG(true), the order is the order of appearance
in the vector of group labels, otherwise alphabetical.)
)
DESCRIPTION(
LANG(welch.test) calculates and prints a display containing
the Welch statistic, p-values for testing the equality of means,
and a confidence interval for the difference in the means.
)
