TITLE(multiple.comp @@ Multiple Comparisons)
USAGE(
multiple.comp(anova)
)
DESCRIPTION(
Calculates and prints multiple confidence intervals
for all possible differences in means in a one-way ANOVA.
LANG(anova) is assumed to be the result of
a one-way ANOVA performed by LANG(anova.table).
)
