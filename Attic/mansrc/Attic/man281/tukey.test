TITLE(tukey.test @@ Tukey One Degree-of-Freedom Test)
USAGE(
tukey.test(anova)
)
DESCRIPTION(
LANG(tukey.test) perfoms a Tukey one-degree of freedom test for no
interaction.  The object LANG(anova) is the list returned by
LANG(anova.table) after calculating the ANOVA table for a two-way
ANOVA with one observation per cell.
PARA
The function prints out the result of the test but returns no value.
)
